import { PlaceholderPage } from "../PlaceholderPage";

export function AdminAuditLogPage() {
  return (
    <PlaceholderPage
      title="Audit Log"
      message="Platform-wide audit log (logins, admin actions) is coming in Phase 2."
    />
  );
}
